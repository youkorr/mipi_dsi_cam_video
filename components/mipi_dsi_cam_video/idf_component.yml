## IDF Component Manager pour mipi_dsi_cam_video
## Intègre esp-video avec tous ses composants

dependencies:
  # esp-video et ses dépendances (sera géré automatiquement par IDF Component Manager)
  espressif/esp_video:
    version: "^1.3.1"
  
  # Les autres dépendances sont automatiquement gérées par esp_video
  # Pas besoin de les lister explicitement sauf si on veut override
  
  # Si vous avez une version locale de esp_cam_sensor avec SC202CS:
  # espressif/esp_cam_sensor:
  #   version: "*"
  #   override_path: "../esp_cam_sensor"

idf:
  version: ">=5.3.0"
